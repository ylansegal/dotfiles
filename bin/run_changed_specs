#! /bin/bash

set -e

(git diff --name-only origin/develop..HEAD; git ls-files -m --other --exclude-standard) | \
grep 'spec.rb' | \
	while read f; do \
		[ -f "$f" ] && echo "$f"; \
	done | \
xargs smart_rspec $@