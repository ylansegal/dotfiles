#!/usr/bin/env bash

bundle show rubocop > /dev/null
if [ $? == 0 ]; then
  RUBOCOP="bundle exec rubocop"
else
  RB_VERSION=$(cat ~/.ruby-version)
  RUBOCOP=~/.rbenv/versions/$RB_VERSION/bin/rubocop
fi

$RUBOCOP $@
