#!/usr/bin/env bash

# Looking for zeus socket
if [ -S ./.zeus.sock ]; then
  RSPEC="zeus rspec"
else
  # Looking for binstubs
  if [ -f ./bin/rspec ]; then
    RSPEC="bin/rspec"
  else
    RSPEC="bundle exec rspec"
  fi
fi

CMD="$RSPEC $@"
echo $CMD
$CMD
